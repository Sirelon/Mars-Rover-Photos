<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:paddingBottom="8dp"
    android:paddingEnd="8dp"
    android:paddingStart="8dp"
    app:cardCornerRadius="4dp">

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:paddingBottom="4dp"
        android:clipToPadding="false"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/photo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:src="@drawable/img_placeholder"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/txtSeeCounter"
            style="@style/TxtCounter"
            android:layout_marginLeft="16dp"
            android:layout_marginStart="16dp"
            android:drawableLeft="@drawable/ic_see_counter"
            android:drawableStart="@drawable/ic_see_counter"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/photo"
            tools:text="See counter" />

        <TextView
            android:id="@+id/txtSaveCounter"
            style="@style/TxtCounter"
            android:drawableLeft="@drawable/ic_save_counter"
            android:drawableStart="@drawable/ic_save_counter"
            app:layout_constraintStart_toStartOf="@id/txtSeeCounter"
            app:layout_constraintTop_toBottomOf="@+id/txtSeeCounter"
            tools:text="Save counter" />

        <TextView
            android:id="@+id/txtScaleCounter"
            style="@style/TxtCounter"
            android:layout_marginEnd="16dp"
            android:layout_marginRight="16dp"
            android:drawableLeft="@drawable/ic_scale_counter"
            android:drawableStart="@drawable/ic_scale_counter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/photo"
            tools:text="Scale counter" />

        <TextView
            android:id="@+id/txtShareCounter"
            style="@style/TxtCounter"
            android:drawableLeft="@drawable/ic_share_counter"
            android:drawableStart="@drawable/ic_share_counter"
            app:layout_constraintEnd_toEndOf="@id/txtScaleCounter"
            app:layout_constraintTop_toBottomOf="@+id/txtScaleCounter"
            tools:text="Scale counter" />

    </android.support.constraint.ConstraintLayout>

</android.support.v7.widget.CardView>